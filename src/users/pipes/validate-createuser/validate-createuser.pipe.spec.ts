import { ValidateCreateuserPipe } from './validate-createuser.pipe';

describe('ValidateCreateuserPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateuserPipe()).toBeDefined();
  });
});
